#!/usr/bin/python3

def max_integer(my_list=[]):
    if len(my_list) == 0:
        return (Nonei)
    max_value = my_list[0]

    for n in my_list:
        if max_value <= n:
            max_value = n

    return (max_value)
