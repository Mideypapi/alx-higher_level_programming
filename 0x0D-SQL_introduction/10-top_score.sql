-- Script to list all records of table second_table displayed by score and name
-- Use SELECT * to print all, ORDER BY to sort, DESC (descending)
SELECT `score`, `name`
FROM second_table
ORDER BY `score` DESC;
